services:
  api:
    build: .
    container_name: iot_api
    ports:
      - "8001:8001"
    env_file:
      - ./.env
    environment:
      - DATABASE_URL=sqlite:///app.db
    command: ["sh", "-c", "uv run alembic upgrade head && uv run uvicorn app.main:app --host 0.0.0.0 --port 8001"]
